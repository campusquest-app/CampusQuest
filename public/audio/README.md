# CampusQuest audio assets

Files here are served at `/audio/...`.

## XP forge fill

**Active file:** `xpforgesound.mp3` (served at `/audio/xpforgesound.mp3`)

Legacy alias: `xp-forge.mp3` (same asset).

The XP overlay plays this during the fill animation (looping at ~26% volume). Unlock happens when the user taps **Scan QR**.

## Optional future assets

- `level-up.mp3`
- `qr-scan-success.mp3`
- `boss-hit.mp3`
